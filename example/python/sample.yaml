name: YAMLStar Example
version: 0.1.0
description: A pure YAML 1.2 loader for Clojure
features:
  - YAML 1.2 spec compliance
  - Pure Clojure implementation
  - Cross-language consistency
config:
  enabled: true
  port: 8080
  timeout: 30
tags:
  - yaml
  - clojure
  - parser
